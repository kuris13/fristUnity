rigidBody 사용 x

1. cameraCenter에서 camera까지 길이를 구한다.

2. cameraCenter에서 camera까지의 방향벡터를 구한다.

3. 내 위치에서 카메라까지의 높이,가로 값으로 카메라 길이만큼 레이를 쏜다

4. Vector3.zero가 아닐 시 -> 레이케이스 성공 
(즉, 카메라가 있어야할 자리에 다른 물체가 있다는 것!) 

5. 레이케스트가 실패 시
( 카메라의 로컬좌표를 0으로 맞춘다.( cameraCenter로 옮김)

6. 카메라를 카메라위치까지의 방향벡터 * 카메라 최대거리 로 옮긴다. 