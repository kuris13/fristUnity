1. 마우스 값을 입력 받아서 cameraCenter값을 변경한다.

2.  cameraCenter값을 이용해서 바디의 각도를 바꾼다(상/하체 동시 변경)

3.  타겟의 벡터로 상체의 forward를 바꾼다.

4. 상체의 로테이션을 바꿔준다.

========================================

1. LookAround() 함수
- 마우스 인풋 값 받기
- cameraCenter.rotation 변경 

-정상 작동 !!

2. LookAround() 함수
- 앞 방향, 옆 방향을 cameraCenter를 이용해서 지정한다.
- 바디의 앞 방향을 지정해준다.

-정상 작동 !!

2-1
- 움직였을 때만 바디의 앞방향을 지정해준다.

- 정상 작동 !!

3. AimScript
- spine = anim.GetBoneTransform(HumanBodyBones.Spine);
- 애니메이션의 척추뼈 부분을 받아온다.
- spine.LookAt(new Vector3(target.position.x, target.position.y, target.position.z)  );
- 상체(척추)는 타겟을 향하도록 한다.

-주의) 처음 타겟의 높이가 상체(척추)랑 같아야 자연스러움 

- 타겟의 position값에 따라 상체만 움직임 !!
->타겟을 조준했을 경우 타겟의 왼쪽을 조준함...
->어째서??
->일단 무시하자

->타겟은 카메라 하위계층에 있어야 따라 움직인다
->타겟이 플레이어 몸체 안에 있지 않다면, 조준이 이상해짐
   (아마 상체의 조준 각도에 문제가 있는듯 = 해결 힘듬)

->타겟은 플레이어의 몸체 안에 있어야함!
-> 따라서 문제는 타겟이 카메라의 하위계층에 있지 않으면서
    카메라의 값을 따라 가야한다.
->해결

-카메라의 이동에 따라 타겟의 이동을 바꿔야 한다.




